import { Link } from "react-router-dom";

import "./styles.css";

const Navbar = () => {
  return (
    <nav className="navbar main-nav bg-primary d-flex align-items-center">
      <div className="container mt-2">
        <h4 className="">GitHub API</h4>
      </div>
    </nav>
  );
};

export default Navbar;
